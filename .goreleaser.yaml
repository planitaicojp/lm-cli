version: 2

builds:
  - binary: lm
    ldflags:
      - -s -w -X github.com/crowdy/lm-cli/cmd.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

brews:
  - repository:
      owner: planitaicojp
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://planitaicojp.github.io/lm-cli-pages/"
    description: "CLI tool for LINE Messaging API"
    license: "Apache-2.0"

scoops:
  - repository:
      owner: planitaicojp
      name: bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://planitaicojp.github.io/lm-cli-pages/"
    description: "CLI tool for LINE Messaging API"
    license: "Apache-2.0"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
